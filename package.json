{
  "name": "between-lines",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "check": "node --check server.js && node --check lib/game.js && node --check lib/ai.js && node --check public/app.js",
    "test": "node --test"
  }
}
